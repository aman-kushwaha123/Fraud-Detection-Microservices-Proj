spring.application.name=FRAUD_CASES_REC-SERVICE
spring.datasource.url=jdbc:mysql://localhost:3306/Fraud_CasesRec_db
spring.datasource.username=root
spring.datasource.password=Aman
server.port=8083
#for hibernate configurations
spring.jpa.hibernate.ddl-auto=update

#eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
# Enable registration with Eureka
#eureka.client.register-with-eureka=true
# Enable fetching the registry (optional for discovery)
#eureka.client.fetch-registry=true

#for showing sql queries on console in well format
spring.jpa.show-sql=true
spring.jpa.properties.format-sql=true


# Kafka broker
spring.kafka.bootstrap-servers=localhost:9092

# Consumer group & offset
spring.kafka.consumer.group-id=fraudCasesConsumer-Group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.max-poll-records=1
spring.kafka.consumer.properties.retry.backoff.ms=500

# Use ErrorHandlingDeserializer
spring.kafka.consumer.key-deserializer=org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.ErrorHandlingDeserializer

# Delegate deserializers
spring.kafka.consumer.properties.spring.deserializer.key.delegate.class=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.properties.spring.deserializer.value.delegate.class=org.springframework.kafka.support.serializer.JsonDeserializer

# JSON config
spring.kafka.consumer.properties.spring.json.trusted.packages=*
spring.kafka.consumer.properties.spring.json.use.type.headers=true

#to show sql queries on console in well format
spring.jpa.show-sql=true
spring.jpa.properties.format-sql=true


 